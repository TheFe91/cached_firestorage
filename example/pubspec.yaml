name: cached_firestorage_demo
description: The DEMO for the CachedFirestorage package

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cached_firestorage:
    path: ../
  cupertino_icons: ^1.0.5
  firebase_core: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
